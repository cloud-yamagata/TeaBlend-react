<Window x:Class="PurchaseTtransfer.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
        xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
        xmlns:v="clr-namespace:PurchaseTtransfer.Views"
        xmlns:vm="clr-namespace:PurchaseTtransfer.ViewModels"
        Title="仕入実績情報一覧" Height="650" Width="1280"  HorizontalAlignment="Center"
        ResizeMode="NoResize" FontSize="16">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <Style x:Key="TextBlockRightStyle" TargetType="{x:Type TextBlock}" BasedOn="{x:Static DataGridTextColumn.DefaultElementStyle}">
            <Setter Property="HorizontalAlignment" Value="Right" />
        </Style>
        <Style x:Key="TextBoxRightStyle" TargetType="{x:Type TextBox}" BasedOn="{x:Static DataGridTextColumn.DefaultEditingElementStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Right" />
        </Style>
    </Window.Resources>

    <i:Interaction.Triggers>

        <!--Viewに特別な要件が存在しない限りは、トリガーやアクションの自作にこだわらず積極的にコードビハインドを使いましょう -->
        <!--Viewのコードビハインドは、基本的にView内で完結するロジックとViewModelからのイベントの受信(専用リスナを使用する)に限るとトラブルが少なくなります -->
        <!--Livet1.1からはコードビハインドでViewModelのイベントを受信するためのWeakEventLisnterサポートが追加されています -->

        <!--WindowのContentRenderedイベントのタイミングでViewModelのInitializeメソッドが呼ばれます-->
        <i:EventTrigger EventName="ContentRendered">
            <l:LivetCallMethodAction MethodTarget="{Binding}" MethodName="Initialize"/>
        </i:EventTrigger>

        <!--Windowが閉じたタイミングでViewModelのDisposeメソッドが呼ばれます-->
        <i:EventTrigger EventName="Closed">
            <l:DataContextDisposeAction/>
        </i:EventTrigger>

        <l:InteractionMessageTrigger Messenger="{Binding Messenger}" MessageKey="DeleteConfirm">
            <l:ConfirmationDialogInteractionMessageAction />
        </l:InteractionMessageTrigger>
        <!--WindowのCloseキャンセル処理に対応する場合は、WindowCloseCancelBehaviorの使用を検討してください-->

        <l:InteractionMessageTrigger Messenger="{Binding Messenger}" MessageKey="EditCommand">
            <l:TransitionInteractionMessageAction
                WindowType="{x:Type v:EditWindow}" Mode="Modal" />
        </l:InteractionMessageTrigger>

        <l:InteractionMessageTrigger Messenger="{Binding Messenger}" MessageKey="DeleteCommand">
            <l:TransitionInteractionMessageAction
                WindowType="{x:Type v:EditWindow}" Mode="Modal" />
        </l:InteractionMessageTrigger>        
        
        <l:InteractionMessageTrigger Messenger="{Binding Messenger}" MessageKey="ListCommand">
            <l:TransitionInteractionMessageAction
                WindowType="{x:Type v:SubWindow}" Mode="Modal" />
        </l:InteractionMessageTrigger>
        <l:InteractionMessageTrigger Messenger="{Binding Messenger}" MessageKey="SubEditCommand">
            <l:TransitionInteractionMessageAction
                WindowType="{x:Type v:SubEditWindow}" Mode="Modal" />
        </l:InteractionMessageTrigger>
        <l:InteractionMessageTrigger Messenger="{Binding Messenger}" MessageKey="ReportCondCommand">
            <l:TransitionInteractionMessageAction
                WindowType="{x:Type v:ReportCondWindow}" Mode="Modal" />
        </l:InteractionMessageTrigger>        
        
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding Stores}" IsReadOnly="True" Grid.Row="3" SelectedItem="{Binding SelectedStore.Value}" SelectionMode="Single" Background="Honeydew">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="Height" Value="50"/>
                    <Setter Property="Background" Value="ForestGreen" />
                    <Setter Property="BorderBrush" Value="White" />
                    <Setter Property="BorderThickness" Value="0.5" />
                    <Setter Property="Foreground" Value="white" />
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTemplateColumn IsReadOnly="True" Header="選&#13;択">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsUpdateSel.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      HorizontalAlignment="Center" VerticalAlignment="Center">
                            </CheckBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn IsReadOnly="True" Header="原&#13;料">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <CheckBox.Style>
                                        <Style TargetType="CheckBox">
                                        <Setter Property="IsEnabled" Value="false"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding is_chk_usable.Value}" Value="True">
                                                    <Setter Property="IsEnabled" Value="True"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </CheckBox.Style>
                            </CheckBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Width="30" Binding="{Binding year.Value}" Header="年&#13;度" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="80" Binding="{Binding bid_no.Value}" Header="入札NO"/>
                <DataGridTextColumn Width="90" Binding="{Binding purchase_date.Value, StringFormat=yyyy/MM/dd}" Header="仕入日"/>
                <DataGridTextColumn Width="80" Binding="{Binding purchase.Value}" Header="仕入先"/>
                <DataGridTextColumn Width="50" Binding="{Binding variety.Value}" Header="品種"/>
                <DataGridTextColumn Width="50" Binding="{Binding tea_life.Value}" Header="茶期"/>
                <DataGridTextColumn Width="50" Binding="{Binding grade.Value}" Header="格付"/>
                <DataGridTextColumn Width="50" Binding="{Binding tea_type.Value}" Header="茶種"/>
                <DataGridTextColumn Width="50" Binding="{Binding tea_rank.Value}" Header="品柄"/>
                <DataGridTextColumn Width="30" Binding="{Binding field_no.Value}" Header="圃&#13;場"/>
                <DataGridTextColumn Width="80" Binding="{Binding producer.Value}" Header="生産者"/>
                <!--<DataGridTextColumn Width="50" Binding="{Binding cost.Value}" Header="原価" ElementStyle="{StaticResource TextBlockRightStyle}"/>-->
                <DataGridTextColumn Width="60" Binding="{Binding unit_weight.Value, StringFormat=N2}" Header="梱包&#13;重量" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="40" Binding="{Binding unit_number.Value, StringFormat=N0}" Header="梱包&#13;数" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="50" Binding="{Binding fraction_weight.Value, StringFormat=N2}" Header="端数&#13;重量" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="40" Binding="{Binding fraction_number.Value, StringFormat=N0}" Header="端数&#13;数" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="60" Binding="{Binding purchase_weight.Value, StringFormat=N2}" Header="仕入&#13;重量" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="40" Binding="{Binding cost.Value, StringFormat=N0}" Header="単価" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="30" Binding="{Binding discount.Value, StringFormat=N2}" Header="粉&#13;引" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="40" Binding="{Binding status.Value}" Header="残量&#13;状況"/>
                <DataGridTextColumn Width="60" Binding="{Binding transfer_quantity.Value, StringFormat=N2}" Header="振分&#13;重量" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <!--<DataGridTextColumn Width="50" Binding="{Binding yield.Value, StringFormat=N2}" Header="粉引" ElementStyle="{StaticResource TextBlockRightStyle}"/>-->
                <DataGridTextColumn Width="80" Binding="{Binding target.Value}" Header="用途"/>
                <DataGridTextColumn Width="80" Binding="{Binding target_plan.Value}" Header="予定&#13;用途"/>
                <DataGridTextColumn Width="60" Binding="{Binding lot_no.Value}" Header="ロット&#13;NO"/>
                <!--<DataGridTextColumn Width="100" Binding="{Binding remarks.Value}" Header="備考"/>-->
                <!--<DataGridTextColumn Width="90" Binding="{Binding transfer_date.Value, StringFormat=yyyy/MM/dd}" Header="振分日"/>
                <DataGridTextColumn Width="50" Binding="{Binding result_type_name.Value}" Header="実績&#13;種別"/>
                <DataGridTextColumn Width="60" Binding="{Binding transfer.Value}" Header="受入/&#13;転売先"/>
                <DataGridTemplateColumn IsReadOnly="True" Header="許可">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding is_chk_usable.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      HorizontalAlignment="Center" VerticalAlignment="Center">
                            </CheckBox>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>-->

            </DataGrid.Columns>
        </DataGrid>
        <Menu Height="30">
            <MenuItem Header="登録" IsEnabled="{Binding AddEnabled.Value}" HorizontalAlignment="Center" Height="30" Width="50" VerticalAlignment="Center">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodTarget="{Binding Mode=OneWay}" MethodName="Regist"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </MenuItem>
            <MenuItem Header="変更" IsEnabled="{Binding EditEnabled.Value}" HorizontalAlignment="Center" Height="30" Width="50" VerticalAlignment="Center">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodTarget="{Binding Mode=OneWay}" MethodName="Edit"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </MenuItem>
            <MenuItem Header="削除" IsEnabled="{Binding DeleteEnabled.Value}" HorizontalAlignment="Center" Height="30" Width="50" VerticalAlignment="Center">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodTarget="{Binding Mode=OneWay}" MethodName="Delete"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <!--<i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:ConfirmationDialogInteractionMessageAction>
                            <l:DirectInteractionMessage CallbackMethodTarget="{Binding Mode=OneWay}" CallbackMethodName="Delete">
                                <l:ConfirmationMessage Button="OKCancel" Caption="確認" Text="削除してもいいですか" />
                            </l:DirectInteractionMessage>
                        </l:ConfirmationDialogInteractionMessageAction>
                    </i:EventTrigger>
                </i:Interaction.Triggers>-->
            </MenuItem>
            <MenuItem Header="一括変更" IsEnabled="{Binding UpdateAllEnabled.Value}" HorizontalAlignment="Center" Height="30" Width="60" VerticalAlignment="Center">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodTarget="{Binding Mode=OneWay}" MethodName="UpdateAll"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </MenuItem>
            <MenuItem Header="振分一覧" IsEnabled="{Binding ListEnabled.Value}" HorizontalAlignment="Center" Height="30" Width="60" VerticalAlignment="Center">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodTarget="{Binding Mode=OneWay}" MethodName="List"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </MenuItem>
            <MenuItem Header="一括振分" IsEnabled="{Binding TransferEnabled.Value}" HorizontalAlignment="Center" Height="30" Width="60" VerticalAlignment="Center">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodTarget="{Binding Mode=OneWay}" MethodName="Transfer"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </MenuItem>
            <MenuItem Header="原料登録" IsEnabled="{Binding MaterialEnabled.Value}" HorizontalAlignment="Center" Height="30" Width="60" VerticalAlignment="Center">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodTarget="{Binding Mode=OneWay}" MethodName="MaterialRegist"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </MenuItem>
            <MenuItem Header="仕入リスト" IsEnabled="{Binding ExcelEnabled.Value}" HorizontalAlignment="Center" Height="30" Width="60" VerticalAlignment="Center">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodTarget="{Binding Mode=OneWay}" MethodName="ExcelReport"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </MenuItem>
            <MenuItem Header="転売リスト" IsEnabled="{Binding ReportEnabled.Value}" HorizontalAlignment="Center" Height="30" Width="60" VerticalAlignment="Center">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodTarget="{Binding Mode=OneWay}" MethodName="ReportCond"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </MenuItem>
            
            <MenuItem Header="CSV取込" IsEnabled="{Binding CSVCaptureEnabled.Value}" HorizontalAlignment="Center" Height="30" Width="60" VerticalAlignment="Center">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodTarget="{Binding Mode=OneWay}" MethodName="CSVCapture"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </MenuItem>
        </Menu>
        <StackPanel Grid.Row="1" Orientation="Horizontal" >
            <Label Content="年度" Background="YellowGreen" VerticalAlignment="Bottom"/>
            <TextBox Text="{Binding year}" Width="30"
                     InputMethod.PreferredImeState="On" InputMethod.PreferredImeConversionMode="Alphanumeric"/>
            <Button Content="年度切替" Width="75" Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="YearChange" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
            <Label Content="キーワード" Background="YellowGreen" VerticalAlignment="Bottom"/>
            <TextBox Text="{Binding sel_name1}" Width="100"/>
            <TextBox Text="{Binding sel_name2}" Width="100"/>
            <TextBox Text="{Binding sel_name3}" Width="100"/>
            <Button Content="検索" Width="75" Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="SelLot" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
            <Label BorderBrush="Black" Content="仕入日" Background="YellowGreen" VerticalAlignment="Bottom"/>
            <DatePicker SelectedDate="{Binding purchase_date}" HorizontalAlignment="Right" Width="120"/>
            <Button Content="検索" Width="75" Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="SelDate" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
            <GroupBox  Width="180" Height="30" Background="PaleGreen" FontSize="14">
                <StackPanel Orientation="Horizontal" Height="30">
                    <CheckBox Margin="0,8,0,0" IsChecked="{Binding isStatusCheck1, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Content="未" Height="30" />
                    <CheckBox Margin="0,8,0,0" IsChecked="{Binding isStatusCheck2, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Content="残" Height="30" />
                    <CheckBox Margin="0,8,0,0" IsChecked="{Binding isStatusCheck3, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Content="完" Height="30" />
                    <CheckBox Margin="0,8,0,0" IsChecked="{Binding isStatusCheck4, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Content="誤" Height="30" />
                </StackPanel>
            </GroupBox>
            <Button Content="検索" Width="50"  Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="SelStatus" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
            <Border Width="5"></Border>
            <Button Content="用途未定" Width="70"  Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="SelNoPlan" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
            <Border Width="5"></Border>
            <Button Content="原料登録" Width="70"  Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="GetMaterial" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal">
        </StackPanel>

    </Grid>
</Window>

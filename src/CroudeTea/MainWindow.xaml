<Window x:Class="CroudeTea.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
        xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
        xmlns:v="clr-namespace:CroudeTea.Views"
        xmlns:vm="clr-namespace:CroudeTea.ViewModels"
        Title="ロット製造実績一覧" Height="600" Width="1000"  HorizontalAlignment="Center"
        ResizeMode="NoResize" FontSize="16">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <Style x:Key="TextBlockRightStyle" TargetType="{x:Type TextBlock}" BasedOn="{x:Static DataGridTextColumn.DefaultElementStyle}">
            <Setter Property="HorizontalAlignment" Value="Right" />
        </Style>
        <Style x:Key="TextBoxRightStyle" TargetType="{x:Type TextBox}" BasedOn="{x:Static DataGridTextColumn.DefaultEditingElementStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Right" />
        </Style>
    </Window.Resources>

    <i:Interaction.Triggers>

        <!--Viewに特別な要件が存在しない限りは、トリガーやアクションの自作にこだわらず積極的にコードビハインドを使いましょう -->
        <!--Viewのコードビハインドは、基本的にView内で完結するロジックとViewModelからのイベントの受信(専用リスナを使用する)に限るとトラブルが少なくなります -->
        <!--Livet1.1からはコードビハインドでViewModelのイベントを受信するためのWeakEventLisnterサポートが追加されています -->

        <!--WindowのContentRenderedイベントのタイミングでViewModelのInitializeメソッドが呼ばれます-->
        <i:EventTrigger EventName="ContentRendered">
            <l:LivetCallMethodAction MethodTarget="{Binding}" MethodName="Initialize"/>
        </i:EventTrigger>

        <!--Windowが閉じたタイミングでViewModelのDisposeメソッドが呼ばれます-->
        <i:EventTrigger EventName="Closed">
            <l:DataContextDisposeAction/>
        </i:EventTrigger>

        <l:InteractionMessageTrigger Messenger="{Binding Messenger}" MessageKey="DeleteConfirm">
            <l:ConfirmationDialogInteractionMessageAction />
        </l:InteractionMessageTrigger>
        <!--WindowのCloseキャンセル処理に対応する場合は、WindowCloseCancelBehaviorの使用を検討してください-->

        <l:InteractionMessageTrigger Messenger="{Binding Messenger}" MessageKey="EditCommand">
            <l:TransitionInteractionMessageAction
                WindowType="{x:Type v:EditWindow}" Mode="Modal" />
        </l:InteractionMessageTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding Stores}" IsReadOnly="True" Grid.Row="3" SelectedItem="{Binding SelectedStore.Value}" SelectionMode="Single"  Background="Honeydew">
           <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="Height" Value="45"/>
                    <Setter Property="Background" Value="ForestGreen" />
                    <Setter Property="BorderBrush" Value="White" />
                    <Setter Property="BorderThickness" Value="0.5" />
                    <Setter Property="Foreground" Value="white" />
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Width="100" Binding="{Binding work_date.Value, StringFormat=yyyy/MM/dd}" Header="製造日"/>
                <DataGridTextColumn Width="50" Binding="{Binding lot_no.Value}" Header="ロット&#13;No" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="50" Binding="{Binding process_type_name.Value}" Header="工程&#13;区分"/>
                <DataGridTextColumn Width="50" Binding="{Binding product_no.Value}" Header="製造&#13;No" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="50" Binding="{Binding lot_status_name.Value}" Header="ロット&#13;状態"/>
                <DataGridTextColumn Width="220" Binding="{Binding lot_name.Value}" Header="ロット名"/>
                <DataGridTextColumn Width="30" Binding="{Binding make_year.Value}" Header="年&#13;度"/>
                <DataGridTextColumn Width="120" Binding="{Binding item_name.Value}" Header="通称名"/>
                <DataGridTextColumn Width="30" Binding="{Binding count.Value}" Header="回&#13;数"/>
                <DataGridTextColumn Width="50" Binding="{Binding organic_class.Value}" Header="有機&#13;区分"/>
                <DataGridTextColumn Width="50" Binding="{Binding unit_weight.Value, StringFormat=N0}" Header="梱包&#13;重量" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="40" Binding="{Binding unit_number.Value, StringFormat=N0}" Header="梱包&#13;数" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="50" Binding="{Binding fraction_weight.Value, StringFormat=N2}" Header="端数&#13;重量" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="85" Binding="{Binding remarks.Value}" Header="摘要"/>
            </DataGrid.Columns>
        </DataGrid>
        <Menu>
            <StackPanel Orientation="Horizontal" Height="30" Background="PaleGreen" Width="230">
                <RadioButton Content="荒茶配合" IsChecked="{Binding isProcessClick02, Mode=TwoWay}" Height="16"/>
                <RadioButton Content="仕上" IsChecked="{Binding isProcessClick03, Mode=TwoWay}" Height="16"/>
                <RadioButton Content="火入" IsChecked="{Binding isProcessClick04, Mode=TwoWay}" Height="16"/>
                <RadioButton Content="仕上配合" IsChecked="{Binding isProcessClick05, Mode=TwoWay}" Height="16"/>
            </StackPanel>
            <MenuItem Header="登録" IsEnabled="{Binding AddEnabled.Value}"  Height="30">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodTarget="{Binding Mode=OneWay}" MethodName="Regist"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </MenuItem>
            <MenuItem Header="変更" IsEnabled="{Binding EditEnabled.Value}"  Height="30">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodTarget="{Binding Mode=OneWay}" MethodName="Edit"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </MenuItem>
        </Menu>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <GroupBox  Width="200" Height="30" Background="PaleGreen" FontSize="14">
                <StackPanel Orientation="Horizontal" Height="30">
                    <RadioButton Content="ALL" IsChecked="{Binding all, Mode=TwoWay}" Height="16"/>
                    <RadioButton Content="仕掛" IsChecked="{Binding active, Mode=TwoWay}" Height="16"/>
                    <RadioButton Content="完了" IsChecked="{Binding complete, Mode=TwoWay}" Height="16"/>
                    <RadioButton Content="確定" IsChecked="{Binding confirm, Mode=TwoWay}" Height="16"/>
                </StackPanel>
            </GroupBox>
            <Button Content="製造実績" Width="80"  Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="History" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>

            <Label BorderBrush="Black" Content="製造日" Background="YellowGreen" VerticalAlignment="Bottom"/>
            <DatePicker SelectedDate="{Binding product_date}" HorizontalAlignment="Right" Width="120"/>
            <Button Content="検索" Width="75" Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="SelDate" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>                    
            <Button Content="本日の作業" Width="80"  Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="Today" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
            <TextBox
                Text="{Binding sel_name, Mode=TwoWay}"              
                HorizontalAlignment="Left" 
                VerticalAlignment="Center" TextAlignment="Left" Height="30" Width="230" IsHitTestVisible="True"/>
            <Button Content="検索" Width="50"  Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="Sel" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>

            <Button Content="再読込" Width="60"  Background="DarkGreen" Foreground="white" Height="31" VerticalAlignment="Top">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="Reload" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal">
            <GroupBox  Width="300" Height="30" Background="PaleGreen" FontSize="14">
                <StackPanel Orientation="Horizontal" Height="30">
                    <CheckBox Margin="0,8,0,0" IsChecked="{Binding isProcessCheck02, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Content="荒茶配合" Height="30" />
                    <CheckBox Margin="0,8,0,0" IsChecked="{Binding isProcessCheck03, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Content="仕上" Height="30" />
                    <CheckBox Margin="0,8,0,0" IsChecked="{Binding isProcessCheck04, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Content="火入" Height="30" />
                    <CheckBox Margin="0,8,0,0" IsChecked="{Binding isProcessCheck05, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Content="仕上配合" Height="30" />
                </StackPanel>
            </GroupBox>
            <GroupBox  Width="220" Height="30" Background="PaleGreen" FontSize="14">
                <StackPanel Orientation="Horizontal" Height="30">
                    <CheckBox Margin="0,8,0,0" IsChecked="{Binding isOrganicCheckA, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Content="有機茶" Height="30" />
                    <CheckBox Margin="0,8,0,0" IsChecked="{Binding isOrganicCheckB, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Content="無農薬" Height="30" />
                    <CheckBox Margin="0,8,0,0" IsChecked="{Binding isOrganicCheckC, UpdateSourceTrigger=PropertyChanged}"/>
                    <Label Content="一般茶" Height="30" />
                </StackPanel>
            </GroupBox>
            
            <Button Content="検索" Width="50"  Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="DetailSel" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>

            <Label Content="通称名" Background="YellowGreen" Height="30" />
            <Border BorderBrush="Black" BorderThickness="1" Height="30" Width="300">
                <Grid>
                    <ListBox ItemsSource="{Binding ItemListView}" Height="30" Background="PaleGreen"/>
                </Grid>
            </Border>
            <!--<Button Content="検索" Width="75" IsEnabled="{Binding AddEnabled.Value}">-->
            <Button Content="検索" Width="50"  Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="SelItem" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
        </StackPanel>
    </Grid>
</Window>

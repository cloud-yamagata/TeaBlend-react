<Window x:Class="PackageReport.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:ei="http://schemas.microsoft.com/expression/2010/interactions"
        xmlns:l="http://schemas.livet-mvvm.net/2011/wpf"
        xmlns:v="clr-namespace:PackageReport.Views"
        xmlns:vm="clr-namespace:PackageReport.ViewModels"
        Title="製造報告書一覧" Height="600" Width="1005"  HorizontalAlignment="Center"
        ResizeMode="NoResize" FontSize="16">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <Style x:Key="TextBlockRightStyle" TargetType="{x:Type TextBlock}" BasedOn="{x:Static DataGridTextColumn.DefaultElementStyle}">
            <Setter Property="HorizontalAlignment" Value="Right" />
        </Style>
        <Style x:Key="TextBoxRightStyle" TargetType="{x:Type TextBox}" BasedOn="{x:Static DataGridTextColumn.DefaultEditingElementStyle}">
            <Setter Property="HorizontalContentAlignment" Value="Right" />
        </Style>
    </Window.Resources>

    <i:Interaction.Triggers>

        <!--Viewに特別な要件が存在しない限りは、トリガーやアクションの自作にこだわらず積極的にコードビハインドを使いましょう -->
        <!--Viewのコードビハインドは、基本的にView内で完結するロジックとViewModelからのイベントの受信(専用リスナを使用する)に限るとトラブルが少なくなります -->
        <!--Livet1.1からはコードビハインドでViewModelのイベントを受信するためのWeakEventLisnterサポートが追加されています -->

        <!--WindowのContentRenderedイベントのタイミングでViewModelのInitializeメソッドが呼ばれます-->
        <i:EventTrigger EventName="ContentRendered">
            <l:LivetCallMethodAction MethodTarget="{Binding}" MethodName="Initialize"/>
        </i:EventTrigger>

        <!--Windowが閉じたタイミングでViewModelのDisposeメソッドが呼ばれます-->
        <i:EventTrigger EventName="Closed">
            <l:DataContextDisposeAction/>
        </i:EventTrigger>

        <l:InteractionMessageTrigger Messenger="{Binding Messenger}" MessageKey="DeleteConfirm">
            <l:ConfirmationDialogInteractionMessageAction />
        </l:InteractionMessageTrigger>
        <!--WindowのCloseキャンセル処理に対応する場合は、WindowCloseCancelBehaviorの使用を検討してください-->

        <l:InteractionMessageTrigger Messenger="{Binding Messenger}" MessageKey="EditCommand">
            <l:TransitionInteractionMessageAction
                WindowType="{x:Type v:EditWindow}" Mode="Modal" />
        </l:InteractionMessageTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding Stocks}" IsReadOnly="True" Grid.Row="2" SelectedItem="{Binding SelectedStock.Value}" SelectionMode="Single"  Background="Honeydew">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="Height" Value="45"/>
                    <Setter Property="Background" Value="ForestGreen" />
                    <Setter Property="BorderBrush" Value="White" />
                    <Setter Property="BorderThickness" Value="0.5" />
                    <Setter Property="Foreground" Value="white" />
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Width="100" Binding="{Binding work_date.Value, StringFormat=yyyy/MM/dd}" Header="製造日"/>
                <DataGridTextColumn Width="80" Binding="{Binding lot_status.Value}" Header="ロット状態"/>
                <DataGridTextColumn Width="50" Binding="{Binding product_no.Value}" Header="製造&#13;No" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="50" Binding="{Binding item_no.Value}" Header="商品&#13;No" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <!--<DataGridTextColumn Width="20" Binding="{Binding organic_class.Value}" Header="茶&#13;区"/>-->
                <DataGridTextColumn Width="60" Binding="{Binding organic_name}" Header="茶区分"/>
                <DataGridTextColumn Width="270" Binding="{Binding product_name.Value}" Header="商品名"/>
                <!--<DataGridTextColumn Width="50" Binding="{Binding complete_quantity.Value}" Header="生産量" ElementStyle="{StaticResource TextBlockRightStyle}"/>-->
                <DataGridTextColumn Width="80" Binding="{Binding complete_quantity_num, StringFormat=N0}" Header="生産量" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <!--
                <DataGridTextColumn Width="50" Binding="{Binding sample_quantity.Value}" Header="サンプル&#13;数" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="50" Binding="{Binding fail_quantity.Value}" Header="不良数" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                -->
                <DataGridTextColumn Width="50" Binding="{Binding grade_no.Value}" Header="格付&#13;No" ElementStyle="{StaticResource TextBlockRightStyle}"/>
                <DataGridTextColumn Width="160" Binding="{Binding part_name.Value}" Header="使用部品名"/>
                <DataGridTextColumn Width="80" Binding="{Binding use_quantity_num, StringFormat=N2}" Header="使用量" ElementStyle="{StaticResource TextBlockRightStyle}"/>
            </DataGrid.Columns>
        </DataGrid>
        <Menu>
            <MenuItem Header="登録" IsEnabled="{Binding AddEnabled.Value}"  Height="30">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodTarget="{Binding Mode=OneWay}" MethodName="Regist"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </MenuItem>
            <MenuItem Header="変更" IsEnabled="{Binding EditEnabled.Value}"  Height="30">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodTarget="{Binding Mode=OneWay}" MethodName="Edit"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </MenuItem>
            <!--
            <MenuItem Header="削除" IsEnabled="{Binding DeleteEnabled.Value}" >
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:ConfirmationDialogInteractionMessageAction>
                            <l:DirectInteractionMessage CallbackMethodTarget="{Binding Mode=OneWay}" CallbackMethodName="Delete">
                                <l:ConfirmationMessage Button="OKCancel" Caption="確認" Text="削除してもいいですか" />
                            </l:DirectInteractionMessage>
                        </l:ConfirmationDialogInteractionMessageAction>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </MenuItem>
            -->
            </Menu>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Content="製造実績" Width="80"  Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="Sel" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
            <Button Content="本日の作業" Width="80"  Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="SelToday" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
            <Label Content="商品名"   Background="YellowGreen" Height="30" />
            <Border BorderBrush="Black" BorderThickness="1" Height="30" Width="350">
                <Grid>
                    <ListBox ItemsSource="{Binding ItemListView}" Height="30" Background="PaleGreen"/>
                </Grid>
            </Border>
            <!--<Button Content="検索" Width="75" IsEnabled="{Binding AddEnabled.Value}">-->

            <Button Content="検索" Width="80"  Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="SelItem" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
            <GroupBox  Width="250" Height="30" Background="PaleGreen" FontSize="14">
                <StackPanel Orientation="Horizontal" Height="30">
                    <RadioButton Content="ALL" IsChecked="{Binding all, Mode=TwoWay}" Height="16"/>
                    <RadioButton Content="仕掛" IsChecked="{Binding active, Mode=TwoWay}" Height="16"/>
                    <RadioButton Content="測定" IsChecked="{Binding measure, Mode=TwoWay}" Height="16"/>
                    <RadioButton Content="完了" IsChecked="{Binding complete, Mode=TwoWay}" Height="16"/>
                    <RadioButton Content="確定" IsChecked="{Binding confirm, Mode=TwoWay}" Height="16"/>
                </StackPanel>
            </GroupBox>
            <Button Content="抽出" Width="80"  Background="DarkGreen" Foreground="white">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="Click">
                        <l:LivetCallMethodAction MethodName="SelStatus" MethodTarget="{Binding Mode=OneWay}"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </Button>
        </StackPanel>

    </Grid>
</Window>
